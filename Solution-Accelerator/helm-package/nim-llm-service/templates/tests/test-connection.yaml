apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nim-llm-service.fullname" . }}-test-connection"
  labels:
    {{- include "nim-llm-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nim-llm-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
